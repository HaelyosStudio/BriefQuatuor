{
  "compilerOptions": {
    /* Projects */
    /* Language and Environment */
    "target": "ES2022",
    /* Prevents compiling if there is a error */
    "noEmitOnError": true,
    /* Modules */
    "moduleResolution": "node",
    "module": "commonjs",
    "rootDir": "Src/TS",
    /* To select the folder where the files to import are located */
    "baseUrl": "Src/TS/Imports",
    /* To choose an alias for the folder where the .ts files to import are located */
    "paths": {
      "@Imports/*": [
        "*"
      ]
    }, /* Specify a set of entries that re-map imports to additional lookup locations. */
    "outDir": "Public/JS",
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    /* Type Checking */
    "strict": true,
    "skipLibCheck": true
  }
}